server:
  port: 8081

spring:
  application:
    name: huxirating
  profiles:
    active: dev

mybatis-plus:
  type-aliases-package: com.huxirating.entity

logging:
  pattern:
    dateformat: mm:ss.SSS
